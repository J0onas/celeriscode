---
interface Props {
 className?: string;
 variant?: 'default' | 'accent' | 'success' | 'outline';
}

const { className = '', variant = 'default' } = Astro.props;

const variants = {
 default: 'border-border-subtle bg-bg-card text-text-secondary',
 accent: 'border-accent/30 bg-accent/10 text-accent',
 success: 'border-success/30 bg-success/10 text-success',
 outline: 'border-border-subtle bg-transparent text-text-secondary',
};
---

<span
 class:list={[
 'inline-flex items-center rounded-full px-3 py-1 text-[11px] font-bold uppercase tracking-wider border transition-colors',
 variants[variant],
 className,
 ]}
>
 <slot />
</span>
